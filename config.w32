// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("cjson", "for cjson support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("cjson", "enable cjson support", "no");

if (PHP_CJSON != "no") {
	EXTENSION("cjson", "cjson.c");
}

